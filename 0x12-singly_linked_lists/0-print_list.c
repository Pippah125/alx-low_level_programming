#include<stdio.h>
#include<stdlib.h>
#include"lists.h"
struct list_s
{
	char *str;
	unsigned int len;
	struct list_s *next;
} list_t;
size_t print_list(const list_t *h)
{
	typedef struct list_s
	{
		char *str;
		unsigned int len;
		struct list_s *next;
	} list_t;
	 list_t *head = NULL
		head = malloc(sizeof(list_t));
	 int count;
	 if( str == NULL);
	 printf("[0] (nil)");
	 else
	 {
		 list_t betty = NULL;
		 betty = head;
		 for(count = 0; betty != NULL; count++);
		 {
			 betty = betty -> next;
		 }
	 }
	 return(count);
}
