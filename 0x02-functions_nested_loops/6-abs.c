#include "main.h"
/**
 * _abs - prints out absolute value of a number
 *
 * @n: number to be printed
 *
 * Return: n or -n
 */
int _abs(int n)
{
	if (("n = 0") && (n > 0))
	{	return (n);
	}
	else
	{	return (-n);
	}
}
