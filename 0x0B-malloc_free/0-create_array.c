#include "main.h"
char *create_array(unsigned int size, char c)
{
	create_array = malloc(sizeof(char) + size of unsigned int);
		for(i = 0; i <= size; i++)
		{
			if size = 0
			{
				return(NULL);
			}


			return(NULL);
		}

