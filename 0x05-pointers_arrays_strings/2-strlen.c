#include "main.h"

/**
 * _strlen(char *s)- show length of string
 * @_strlen(char *s): show length of string
 * @s: the string to be increased
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int num;

		for ( num = 0; s[num] != '\0'; num++)
		{
			return(num);
		}
	return (num);
}
